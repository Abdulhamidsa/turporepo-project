{
  "name": "turporepo-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=express-app",
    "build": "pnpm clean && turbo run build --filter=express-app",
    "start": "pnpm --filter express-app start",
    "lint": "turbo run lint --filter=express-app",
    "clean": "turbo run clean --filter=express-app"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript": "workspace:*",
    "eslint": "catalog:eslint",
    "prettier": "catalog:prettier",
    "turbo": "^2.3.3",
    "typescript": "catalog:typescript"
  },
  "packageManager": "pnpm@9.12.0",
  "dependencies": {
    "zod": "catalog:zod"
  }
}
